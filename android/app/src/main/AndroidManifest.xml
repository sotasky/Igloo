<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Igloo"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Igloo"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              In-app igloo:// deep links.
              Scoped hosts map 1:1 to the nav graph (see 00-architecture.md §6):
                feed/videos/moments/bookmarks/liked  — tab roots
                channel/{channel_id}                  — unified channel page
                youtube/{video_id}                         — long-form YouTube player
                tt/{video_id}                         — Moments positioned on TikTok
                ig/{video_id}                         — Moments positioned on Instagram
                tw/{tweet_id}                         — Feed scrolled to tweet
              CATEGORY_BROWSABLE kept so igloo:// links clicked from other apps open here;
              https://x.com/* / youtube.com/* intercept is a flagged future project.
            -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="igloo" android:host="feed" />
                <data android:scheme="igloo" android:host="videos" />
                <data android:scheme="igloo" android:host="moments" />
                <data android:scheme="igloo" android:host="bookmarks" />
                <data android:scheme="igloo" android:host="liked" />
                <data android:scheme="igloo" android:host="channel" />
                <data android:scheme="igloo" android:host="youtube" />
                <data android:scheme="igloo" android:host="tt" />
                <data android:scheme="igloo" android:host="ig" />
                <data android:scheme="igloo" android:host="tw" />
            </intent-filter>
        </activity>

        <service
            android:name=".media.MediaForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Android 14+ requires foregroundServiceType on the service element of
             every foreground service. WorkManager's manifest contribution doesn't
             set one, so without this merge override the system kills any worker
             that calls setForeground(...) — including PeriodicSyncWorker. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>
