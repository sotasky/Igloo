-- Code generated by TestSchemaSnapshot. DO NOT EDIT.
-- Update with: go test ./internal/db -run TestSchemaSnapshot -update -count=1

-- index: idx_android_sync_heads_revision on android_sync_heads
CREATE UNIQUE INDEX idx_android_sync_heads_revision ON android_sync_heads(revision);

-- index: idx_android_sync_health_cursor on android_sync_health_reports
CREATE INDEX idx_android_sync_health_cursor ON android_sync_health_reports(cursor, reported_at_ms DESC);

-- index: idx_android_sync_health_reported on android_sync_health_reports
CREATE INDEX idx_android_sync_health_reported ON android_sync_health_reports(reported_at_ms DESC, id DESC);

-- index: idx_assets_desired_object on assets
CREATE INDEX idx_assets_desired_object ON assets(desired_object_id);

-- index: idx_assets_object on assets
CREATE INDEX idx_assets_object ON assets(object_id);

-- index: idx_assets_owner on assets
CREATE INDEX idx_assets_owner ON assets(owner_kind, owner_id, asset_kind, media_index);

-- index: idx_auth_refresh_session on auth_refresh_tokens
CREATE INDEX idx_auth_refresh_session ON auth_refresh_tokens(session_id);

-- index: idx_auth_sessions_user on auth_sessions
CREATE INDEX idx_auth_sessions_user ON auth_sessions(username, revoked);

-- index: idx_bookmarks_date on bookmarks
CREATE INDEX idx_bookmarks_date ON bookmarks(bookmarked_at DESC);

-- index: idx_channel_profiles_platform on channel_profiles
CREATE INDEX idx_channel_profiles_platform ON channel_profiles(platform);

-- index: idx_channel_profiles_refresh on channel_profiles
CREATE INDEX idx_channel_profiles_refresh ON channel_profiles(tombstone, fetched_at) WHERE tombstone = 0;

-- index: idx_channel_profiles_twitter_handle on channel_profiles
CREATE INDEX idx_channel_profiles_twitter_handle ON channel_profiles(LOWER(COALESCE(handle, ''))) WHERE platform = 'twitter' AND tombstone = 0;

-- index: idx_channels_platform on channels
CREATE INDEX idx_channels_platform ON channels(platform);

-- index: idx_download_queue_lease on download_queue
CREATE INDEX idx_download_queue_lease ON download_queue(status, lease_until_ms);

-- index: idx_download_queue_ready on download_queue
CREATE INDEX idx_download_queue_ready ON download_queue(status, next_attempt_at_ms, lease_until_ms, added_at_ms);

-- index: idx_downloader_operations_recent on downloader_operations
CREATE INDEX idx_downloader_operations_recent ON downloader_operations(started_at_ms DESC, id DESC);

-- index: idx_downloader_operations_summary on downloader_operations
CREATE INDEX idx_downloader_operations_summary ON downloader_operations(platform, operation, status, error_kind);

-- index: idx_feed_item_sources_source on feed_item_sources
CREATE INDEX idx_feed_item_sources_source ON feed_item_sources(source_id, last_seen_at DESC);

-- index: idx_feed_items_algo on feed_items
CREATE INDEX idx_feed_items_algo ON feed_items(algo_interest DESC, published_at DESC);

-- index: idx_feed_items_author_fetched on feed_items
CREATE INDEX idx_feed_items_author_fetched ON feed_items(channel_id, fetched_at DESC, published_at DESC, tweet_id DESC) WHERE channel_id IS NOT NULL AND channel_id != '';

-- index: idx_feed_items_canonical_tweet on feed_items
CREATE INDEX idx_feed_items_canonical_tweet ON feed_items(canonical_tweet_id) WHERE canonical_tweet_id IS NOT NULL AND canonical_tweet_id != '';

-- index: idx_feed_items_channel on feed_items
CREATE INDEX idx_feed_items_channel ON feed_items(channel_id, published_at DESC);

-- index: idx_feed_items_content_hash on feed_items
CREATE INDEX idx_feed_items_content_hash ON feed_items(content_hash) WHERE content_hash IS NOT NULL AND content_hash != '';

-- index: idx_feed_items_fetched on feed_items
CREATE INDEX idx_feed_items_fetched ON feed_items(fetched_at DESC, tweet_id DESC);

-- index: idx_feed_items_media_author on feed_items
CREATE INDEX idx_feed_items_media_author ON feed_items(channel_id, published_at DESC) WHERE media_json IS NOT NULL AND media_json != '' AND media_json != '[]' AND is_retweet = 0;

-- index: idx_feed_items_published on feed_items
CREATE INDEX idx_feed_items_published ON feed_items(published_at DESC, tweet_id DESC);

-- index: idx_feed_items_quote on feed_items
CREATE INDEX idx_feed_items_quote ON feed_items(quote_tweet_id) WHERE quote_tweet_id IS NOT NULL AND quote_tweet_id != '';

-- index: idx_feed_items_quote_channel on feed_items
CREATE INDEX idx_feed_items_quote_channel ON feed_items(quote_channel_id, published_at DESC) WHERE quote_channel_id IS NOT NULL AND quote_channel_id != '';

-- index: idx_feed_items_reply_channel on feed_items
CREATE INDEX idx_feed_items_reply_channel ON feed_items(reply_channel_id, published_at DESC) WHERE reply_channel_id IS NOT NULL AND reply_channel_id != '';

-- index: idx_feed_items_reply_parent on feed_items
CREATE INDEX idx_feed_items_reply_parent ON feed_items(reply_to_status, published_at, tweet_id) WHERE reply_to_status IS NOT NULL AND reply_to_status != '';

-- index: idx_feed_items_reposter_channel on feed_items
CREATE INDEX idx_feed_items_reposter_channel ON feed_items(reposter_channel_id, published_at DESC) WHERE reposter_channel_id IS NOT NULL AND reposter_channel_id != '';

-- index: idx_feed_items_source_channel on feed_items
CREATE INDEX idx_feed_items_source_channel ON feed_items(source_channel_id);

-- index: idx_feed_items_unscored on feed_items
CREATE INDEX idx_feed_items_unscored ON feed_items(algo_scored_at) WHERE algo_scored_at = 0;

-- index: idx_feed_rank_snapshot_at on feed_rank_snapshot
CREATE INDEX idx_feed_rank_snapshot_at ON feed_rank_snapshot(computed_at);

-- index: idx_feed_rank_snapshot_pos on feed_rank_snapshot
CREATE INDEX idx_feed_rank_snapshot_pos ON feed_rank_snapshot(rank_position);

-- index: idx_feed_rank_snapshot_score on feed_rank_snapshot
CREATE INDEX idx_feed_rank_snapshot_score ON feed_rank_snapshot(final_score DESC);

-- index: idx_feed_sources_platform on feed_sources
CREATE INDEX idx_feed_sources_platform ON feed_sources(platform, enabled);

-- index: idx_media_objects_claim on media_objects
CREATE INDEX idx_media_objects_claim ON media_objects(job_state, next_attempt_at_ms, lease_until_ms, storage_class, updated_at_ms);

-- index: idx_media_objects_ready_file_path on media_objects
CREATE INDEX idx_media_objects_ready_file_path ON media_objects(file_path) WHERE published_revision > 0;

-- index: idx_moment_views_date on moment_views
CREATE INDEX idx_moment_views_date ON moment_views(viewed_at DESC);

-- index: idx_profile_jobs_claim on profile_jobs
CREATE INDEX idx_profile_jobs_claim ON profile_jobs(completed_revision, requested_revision, next_attempt_at_ms, lease_until_ms, requested_at_ms);

-- index: idx_translation_jobs_ready on translation_jobs
CREATE INDEX idx_translation_jobs_ready ON translation_jobs(status, next_attempt_at, priority, updated_at);

-- index: idx_video_desires_source_position on video_desires
CREATE INDEX idx_video_desires_source_position ON video_desires(source_channel_id, source_component, source_position, video_id);

-- index: idx_video_desires_video on video_desires
CREATE INDEX idx_video_desires_video ON video_desires(video_id, lane, source_position);

-- index: idx_video_repost_sources_reposter on video_repost_sources
CREATE INDEX idx_video_repost_sources_reposter ON video_repost_sources(reposter_channel_id);

-- index: idx_video_repost_sources_time on video_repost_sources
CREATE INDEX idx_video_repost_sources_time ON video_repost_sources(reposted_at_ms DESC, first_seen_at_ms DESC);

-- index: idx_video_repost_sources_video on video_repost_sources
CREATE INDEX idx_video_repost_sources_video ON video_repost_sources(video_id);

-- index: idx_videos_channel_published on videos
CREATE INDEX idx_videos_channel_published ON videos(channel_id, published_at DESC);

-- index: idx_videos_media_shape on videos
CREATE INDEX idx_videos_media_shape ON videos(media_kind, slide_count);

-- index: idx_videos_source_kind on videos
CREATE INDEX idx_videos_source_kind ON videos(source_kind, published_at DESC);

-- table: analytics_events on analytics_events
CREATE TABLE analytics_events ( event_id TEXT PRIMARY KEY, event_type TEXT NOT NULL, timestamp_ms INTEGER NOT NULL, screen TEXT, content_type TEXT, elapsed_ms INTEGER DEFAULT 0, extra_json TEXT );

-- table: analytics_rollups_daily on analytics_rollups_daily
CREATE TABLE analytics_rollups_daily ( day TEXT NOT NULL, event_type TEXT NOT NULL, screen TEXT NOT NULL DEFAULT '', content_type TEXT NOT NULL DEFAULT '', count INTEGER DEFAULT 0, total_elapsed_ms INTEGER DEFAULT 0, PRIMARY KEY (day, event_type, screen, content_type) );

-- table: android_feed_retention on android_feed_retention
CREATE TABLE android_feed_retention ( id INTEGER PRIMARY KEY CHECK (id = 1), feed_days INTEGER NOT NULL, reconciled_at_ms INTEGER NOT NULL );

-- table: android_sync_clock on android_sync_clock
CREATE TABLE android_sync_clock ( id INTEGER PRIMARY KEY CHECK (id = 1), epoch TEXT NOT NULL, revision INTEGER NOT NULL DEFAULT 0 );

-- table: android_sync_heads on android_sync_heads
CREATE TABLE android_sync_heads ( owner_kind TEXT NOT NULL, owner_id TEXT NOT NULL, revision INTEGER NOT NULL, PRIMARY KEY (owner_kind, owner_id) );

-- table: android_sync_health_reports on android_sync_health_reports
CREATE TABLE android_sync_health_reports ( id INTEGER PRIMARY KEY AUTOINCREMENT, cursor TEXT NOT NULL, reported_at_ms INTEGER NOT NULL, payload_json TEXT NOT NULL, verified_assets INTEGER NOT NULL DEFAULT 0, pending_assets INTEGER NOT NULL DEFAULT 0, missing_assets INTEGER NOT NULL DEFAULT 0, total_assets INTEGER NOT NULL DEFAULT 0, verified_bytes INTEGER NOT NULL DEFAULT 0 );

-- table: assets on assets
CREATE TABLE assets ( id INTEGER PRIMARY KEY AUTOINCREMENT, asset_id TEXT UNIQUE NOT NULL, asset_kind TEXT NOT NULL, owner_kind TEXT NOT NULL, owner_id TEXT NOT NULL, media_index INTEGER NOT NULL DEFAULT 0, object_id TEXT NOT NULL, desired_object_id TEXT NOT NULL, lifecycle_state TEXT NOT NULL DEFAULT 'active' CHECK(lifecycle_state IN ('active', 'pruned')), revision INTEGER NOT NULL DEFAULT 1, is_auto INTEGER, audio_language TEXT NOT NULL DEFAULT '', required_reason TEXT NOT NULL DEFAULT '', created_at_ms INTEGER NOT NULL DEFAULT 0, updated_at_ms INTEGER NOT NULL DEFAULT 0, FOREIGN KEY (object_id) REFERENCES media_objects(object_id), FOREIGN KEY (desired_object_id) REFERENCES media_objects(object_id), UNIQUE(asset_kind, owner_kind, owner_id, media_index) );

-- table: auth_refresh_tokens on auth_refresh_tokens
CREATE TABLE auth_refresh_tokens ( token_id TEXT PRIMARY KEY, session_id TEXT NOT NULL, issued_at_ms INTEGER NOT NULL, expires_at_ms INTEGER NOT NULL, consumed_at_ms INTEGER, FOREIGN KEY (session_id) REFERENCES auth_sessions(session_id) ON DELETE CASCADE );

-- table: auth_sessions on auth_sessions
CREATE TABLE auth_sessions ( session_id TEXT PRIMARY KEY, username TEXT NOT NULL, created_at_ms INTEGER NOT NULL, last_active_at_ms INTEGER NOT NULL, revoked INTEGER NOT NULL DEFAULT 0, revoke_reason TEXT );

-- table: bookmark_categories on bookmark_categories
CREATE TABLE bookmark_categories ( id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, archive_path TEXT, created_at INTEGER NOT NULL DEFAULT 0 );

-- table: bookmarks on bookmarks
CREATE TABLE bookmarks ( video_id TEXT PRIMARY KEY, category_id INTEGER NOT NULL DEFAULT 0, custom_title TEXT, account_handles TEXT, media_indices TEXT, bookmarked_at INTEGER NOT NULL DEFAULT 0 );

-- table: category_create_receipts on category_create_receipts
CREATE TABLE category_create_receipts ( request_id TEXT PRIMARY KEY, category_id INTEGER NOT NULL, provisional_id TEXT NOT NULL, created_at_ms INTEGER NOT NULL );

-- table: channel_follows on channel_follows
CREATE TABLE channel_follows ( channel_id TEXT PRIMARY KEY, followed_at INTEGER NOT NULL DEFAULT 0 );

-- table: channel_profiles on channel_profiles
CREATE TABLE channel_profiles ( channel_id TEXT PRIMARY KEY, platform TEXT NOT NULL, handle TEXT, display_name TEXT, bio TEXT, website TEXT, followers INTEGER DEFAULT 0, following INTEGER DEFAULT 0, verified INTEGER DEFAULT 0, verified_type TEXT, protected INTEGER DEFAULT 0, observed_at_ms INTEGER NOT NULL DEFAULT 0, fetched_at INTEGER NOT NULL DEFAULT 0, tombstone INTEGER DEFAULT 0 );

-- table: channel_settings on channel_settings
CREATE TABLE channel_settings ( channel_id TEXT PRIMARY KEY, media_only INTEGER, include_reposts INTEGER, media_download_limit INTEGER, max_videos INTEGER, download_subtitles INTEGER, updated_at INTEGER NOT NULL DEFAULT 0 );

-- table: channel_stars on channel_stars
CREATE TABLE channel_stars ( channel_id TEXT PRIMARY KEY, starred_at INTEGER NOT NULL DEFAULT 0 );

-- table: channels on channels
CREATE TABLE channels ( id INTEGER PRIMARY KEY AUTOINCREMENT, channel_id TEXT UNIQUE NOT NULL, source_id TEXT, name TEXT NOT NULL, url TEXT, platform TEXT, quality TEXT, last_checked INTEGER NOT NULL DEFAULT 0, created_at INTEGER NOT NULL DEFAULT 0 );

-- table: download_queue on download_queue
CREATE TABLE download_queue ( video_id TEXT PRIMARY KEY, owner_channel_id TEXT NOT NULL, title TEXT NOT NULL DEFAULT '', published_at_ms INTEGER NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending', 'processing', 'blocked')), retry_count INTEGER NOT NULL DEFAULT 0, next_attempt_at_ms INTEGER NOT NULL DEFAULT 0, last_error_kind TEXT NOT NULL DEFAULT '', last_error TEXT NOT NULL DEFAULT '', lease_owner TEXT NOT NULL DEFAULT '', lease_until_ms INTEGER NOT NULL DEFAULT 0, added_at_ms INTEGER NOT NULL DEFAULT 0, started_at_ms INTEGER NOT NULL DEFAULT 0 );

-- table: downloader_operations on downloader_operations
CREATE TABLE downloader_operations ( id INTEGER PRIMARY KEY AUTOINCREMENT, operation TEXT NOT NULL, platform TEXT NOT NULL, subject TEXT NOT NULL DEFAULT '', tool TEXT NOT NULL, started_at_ms INTEGER NOT NULL, ended_at_ms INTEGER NOT NULL, status TEXT NOT NULL, error_kind TEXT NOT NULL DEFAULT '', error TEXT NOT NULL DEFAULT '', cookie_label TEXT NOT NULL DEFAULT '', elapsed_ms INTEGER NOT NULL DEFAULT 0, item_count INTEGER NOT NULL DEFAULT 0, media_count INTEGER NOT NULL DEFAULT 0, file_count INTEGER NOT NULL DEFAULT 0, bytes INTEGER NOT NULL DEFAULT 0, summary_json TEXT NOT NULL DEFAULT '' );

-- table: feed_item_sources on feed_item_sources
CREATE TABLE feed_item_sources ( tweet_id TEXT NOT NULL, source_id TEXT NOT NULL, first_seen_at INTEGER NOT NULL DEFAULT 0, last_seen_at INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (tweet_id, source_id), FOREIGN KEY (source_id) REFERENCES feed_sources(source_id) ON DELETE CASCADE );

-- table: feed_items on feed_items
CREATE TABLE feed_items ( tweet_id TEXT PRIMARY KEY, source_channel_id TEXT, channel_id TEXT, body_text TEXT, lang TEXT, is_retweet INTEGER DEFAULT 0, quote_tweet_id TEXT, quote_channel_id TEXT, quote_body_text TEXT, quote_lang TEXT, quote_media_json TEXT, media_json TEXT, canonical_url TEXT, reply_channel_id TEXT, reply_to_status TEXT, reposter_channel_id TEXT, is_reply INTEGER DEFAULT 0, is_ghost INTEGER DEFAULT 0, quote_published_at INTEGER NOT NULL DEFAULT 0, views INTEGER, likes INTEGER, retweets INTEGER, published_at INTEGER NOT NULL DEFAULT 0, fetched_at INTEGER NOT NULL DEFAULT 0, content_hash TEXT, canonical_tweet_id TEXT, algo_interest REAL DEFAULT 0, algo_scored_at INTEGER DEFAULT 0 );

-- table: feed_likes on feed_likes
CREATE TABLE feed_likes ( tweet_id TEXT PRIMARY KEY, liked_at INTEGER NOT NULL DEFAULT 0 );

-- table: feed_rank_snapshot on feed_rank_snapshot
CREATE TABLE feed_rank_snapshot ( tweet_id TEXT PRIMARY KEY, rank_position INTEGER NOT NULL, base_score REAL NOT NULL, decay_factor REAL NOT NULL, freshness_bonus REAL NOT NULL, jitter REAL NOT NULL, diversity_demoted_by REAL NOT NULL DEFAULT 0, final_score REAL NOT NULL, computed_at INTEGER NOT NULL );

-- table: feed_rank_snapshot_history on feed_rank_snapshot_history
CREATE TABLE feed_rank_snapshot_history ( computed_at INTEGER NOT NULL, tweet_id TEXT NOT NULL, rank_position INTEGER NOT NULL, base_score REAL NOT NULL, decay_factor REAL NOT NULL, freshness_bonus REAL NOT NULL, jitter REAL NOT NULL, diversity_demoted_by REAL NOT NULL DEFAULT 0, final_score REAL NOT NULL, PRIMARY KEY (computed_at, tweet_id), UNIQUE (computed_at, rank_position) );

-- table: feed_seen on feed_seen
CREATE TABLE feed_seen ( tweet_id TEXT PRIMARY KEY, seen_at INTEGER NOT NULL DEFAULT 0 );

-- table: feed_share_account_affinity on feed_share_account_affinity
CREATE TABLE feed_share_account_affinity ( handle TEXT PRIMARY KEY, score REAL DEFAULT 0, last_event_at_ms INTEGER, event_count INTEGER DEFAULT 0 );

-- table: feed_share_token_affinity on feed_share_token_affinity
CREATE TABLE feed_share_token_affinity ( token TEXT PRIMARY KEY, score REAL DEFAULT 0, last_event_at_ms INTEGER, event_count INTEGER DEFAULT 0 );

-- table: feed_sources on feed_sources
CREATE TABLE feed_sources ( source_id TEXT PRIMARY KEY, platform TEXT NOT NULL, source_type TEXT NOT NULL, external_id TEXT NOT NULL, label TEXT NOT NULL, url TEXT NOT NULL, enabled INTEGER NOT NULL DEFAULT 1, last_checked INTEGER, last_ok INTEGER, last_error TEXT NOT NULL DEFAULT '', created_at INTEGER NOT NULL DEFAULT 0, updated_at INTEGER NOT NULL DEFAULT 0 );

-- table: ingest_state on ingest_state
CREATE TABLE ingest_state ( handle TEXT PRIMARY KEY, fail_count INTEGER DEFAULT 0, next_retry_at REAL, last_success_at REAL, last_attempt_at REAL, last_error TEXT, last_http_status INTEGER, avg_latency_ms REAL, updated_at INTEGER NOT NULL DEFAULT 0 );

-- table: media_objects on media_objects
CREATE TABLE media_objects ( id INTEGER PRIMARY KEY AUTOINCREMENT, object_id TEXT UNIQUE NOT NULL, object_key TEXT UNIQUE NOT NULL, source_url TEXT NOT NULL DEFAULT '', published_source_url TEXT NOT NULL DEFAULT '', storage_class TEXT NOT NULL CHECK(storage_class IN ('state_ssd', 'bulk_hdd')), desired_revision INTEGER NOT NULL DEFAULT 1, published_revision INTEGER NOT NULL DEFAULT 0, file_path TEXT NOT NULL DEFAULT '', content_type TEXT NOT NULL DEFAULT '', size_bytes INTEGER NOT NULL DEFAULT 0, file_mtime_ns INTEGER NOT NULL DEFAULT 0, job_state TEXT NOT NULL DEFAULT 'queued', last_error_kind TEXT NOT NULL DEFAULT '', last_error TEXT NOT NULL DEFAULT '', attempts INTEGER NOT NULL DEFAULT 0, next_attempt_at_ms INTEGER NOT NULL DEFAULT 0, lease_owner TEXT NOT NULL DEFAULT '', lease_until_ms INTEGER NOT NULL DEFAULT 0, created_at_ms INTEGER NOT NULL DEFAULT 0, updated_at_ms INTEGER NOT NULL DEFAULT 0 );

-- table: moment_views on moment_views
CREATE TABLE moment_views ( video_id TEXT PRIMARY KEY, viewed_at INTEGER NOT NULL DEFAULT 0 );

-- table: moments_cursors on moments_cursors
CREATE TABLE moments_cursors ( scope TEXT PRIMARY KEY CHECK (scope IN ('all', 'following', 'stories')), video_id TEXT NOT NULL, position_ms INTEGER NOT NULL DEFAULT 0, sort_at_ms INTEGER NOT NULL DEFAULT 0, updated_at_ms INTEGER NOT NULL DEFAULT 0 );

-- table: mutation_clocks on mutation_clocks
CREATE TABLE mutation_clocks ( kind TEXT NOT NULL CHECK (kind IN ('like', 'bookmark', 'follow', 'star', 'mute', 'progress')), item_key TEXT NOT NULL, action TEXT NOT NULL CHECK (action IN ('set', 'clear')), updated_at_ms INTEGER NOT NULL, PRIMARY KEY (kind, item_key) ) WITHOUT ROWID;

-- table: muted_channels on muted_channels
CREATE TABLE muted_channels ( channel_id TEXT PRIMARY KEY, muted_at INTEGER NOT NULL DEFAULT 0 );

-- table: profile_jobs on profile_jobs
CREATE TABLE profile_jobs ( channel_id TEXT PRIMARY KEY, requested_revision INTEGER NOT NULL DEFAULT 1, completed_revision INTEGER NOT NULL DEFAULT 0, requested_at_ms INTEGER NOT NULL DEFAULT 0, lease_owner TEXT NOT NULL DEFAULT '', lease_until_ms INTEGER NOT NULL DEFAULT 0, attempts INTEGER NOT NULL DEFAULT 0, next_attempt_at_ms INTEGER NOT NULL DEFAULT 0, last_error TEXT NOT NULL DEFAULT '', updated_at_ms INTEGER NOT NULL DEFAULT 0, FOREIGN KEY (channel_id) REFERENCES channel_profiles(channel_id) ON DELETE CASCADE );

-- table: retweet_sources on retweet_sources
CREATE TABLE retweet_sources ( content_hash TEXT NOT NULL, retweeter_channel_id TEXT NOT NULL, tweet_id TEXT NOT NULL, published_at INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (content_hash, retweeter_channel_id) );

-- table: search_channels_fts on search_channels_fts
CREATE VIRTUAL TABLE search_channels_fts USING fts5( channel_id_pk UNINDEXED, name, source_id, display_name, handle, tokenize = 'unicode61' );

-- table: search_channels_fts_config on search_channels_fts_config
CREATE TABLE 'search_channels_fts_config'(k PRIMARY KEY, v) WITHOUT ROWID;

-- table: search_channels_fts_content on search_channels_fts_content
CREATE TABLE 'search_channels_fts_content'(id INTEGER PRIMARY KEY, c0, c1, c2, c3, c4);

-- table: search_channels_fts_data on search_channels_fts_data
CREATE TABLE 'search_channels_fts_data'(id INTEGER PRIMARY KEY, block BLOB);

-- table: search_channels_fts_docsize on search_channels_fts_docsize
CREATE TABLE 'search_channels_fts_docsize'(id INTEGER PRIMARY KEY, sz BLOB);

-- table: search_channels_fts_idx on search_channels_fts_idx
CREATE TABLE 'search_channels_fts_idx'(segid, term, pgno, PRIMARY KEY(segid, term)) WITHOUT ROWID;

-- table: search_videos_fts on search_videos_fts
CREATE VIRTUAL TABLE search_videos_fts USING fts5( video_id_pk UNINDEXED, title, dearrow_title, dearrow_title_casual, channel_name, tokenize = 'unicode61' );

-- table: search_videos_fts_config on search_videos_fts_config
CREATE TABLE 'search_videos_fts_config'(k PRIMARY KEY, v) WITHOUT ROWID;

-- table: search_videos_fts_content on search_videos_fts_content
CREATE TABLE 'search_videos_fts_content'(id INTEGER PRIMARY KEY, c0, c1, c2, c3, c4);

-- table: search_videos_fts_data on search_videos_fts_data
CREATE TABLE 'search_videos_fts_data'(id INTEGER PRIMARY KEY, block BLOB);

-- table: search_videos_fts_docsize on search_videos_fts_docsize
CREATE TABLE 'search_videos_fts_docsize'(id INTEGER PRIMARY KEY, sz BLOB);

-- table: search_videos_fts_idx on search_videos_fts_idx
CREATE TABLE 'search_videos_fts_idx'(segid, term, pgno, PRIMARY KEY(segid, term)) WITHOUT ROWID;

-- table: settings on settings
CREATE TABLE settings ( key TEXT NOT NULL, value TEXT, PRIMARY KEY (key) );

-- table: sponsorblock_checked on sponsorblock_checked
CREATE TABLE sponsorblock_checked ( video_id TEXT PRIMARY KEY, checked_at INTEGER NOT NULL DEFAULT 0, video_age_at_check TEXT );

-- table: sponsorblock_segments on sponsorblock_segments
CREATE TABLE sponsorblock_segments ( video_id TEXT NOT NULL, start_time REAL NOT NULL, end_time REAL NOT NULL, category TEXT NOT NULL, PRIMARY KEY (video_id, start_time) );

-- table: translation_jobs on translation_jobs
CREATE TABLE translation_jobs ( tweet_id TEXT NOT NULL, field TEXT NOT NULL, target_lang TEXT NOT NULL, source_hash TEXT NOT NULL DEFAULT '', status TEXT NOT NULL DEFAULT 'queued', priority INTEGER NOT NULL DEFAULT 0, attempts INTEGER NOT NULL DEFAULT 0, next_attempt_at INTEGER NOT NULL DEFAULT 0, last_error_kind TEXT NOT NULL DEFAULT '', last_error TEXT NOT NULL DEFAULT '', created_at INTEGER NOT NULL DEFAULT 0, updated_at INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (tweet_id, field, target_lang) );

-- table: translations on translations
CREATE TABLE translations ( tweet_id TEXT NOT NULL, field TEXT NOT NULL, source_lang TEXT NOT NULL, target_lang TEXT NOT NULL, translated_text TEXT NOT NULL, translated_at INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (tweet_id, field, target_lang) );

-- table: video_comments on video_comments
CREATE TABLE video_comments ( video_id TEXT NOT NULL, comment_id TEXT NOT NULL, parent_id TEXT, author_name TEXT, author_id TEXT, text TEXT, like_count INTEGER, published_at INTEGER NOT NULL DEFAULT 0, PRIMARY KEY(video_id, comment_id) ) WITHOUT ROWID;

-- table: video_desires on video_desires
CREATE TABLE video_desires ( source_channel_id TEXT NOT NULL, source_component TEXT NOT NULL, video_id TEXT NOT NULL, source_position INTEGER NOT NULL DEFAULT 0, lane TEXT NOT NULL CHECK(lane IN ('current', 'backfill')), PRIMARY KEY (source_channel_id, source_component, video_id) ) WITHOUT ROWID;

-- table: video_repost_sources on video_repost_sources
CREATE TABLE video_repost_sources ( video_id TEXT NOT NULL, reposter_channel_id TEXT NOT NULL, reposted_at_ms INTEGER NOT NULL DEFAULT 0, first_seen_at_ms INTEGER NOT NULL DEFAULT 0, updated_at_ms INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (video_id, reposter_channel_id) );

-- table: videos on videos
CREATE TABLE videos ( id INTEGER PRIMARY KEY AUTOINCREMENT, video_id TEXT UNIQUE NOT NULL, channel_id TEXT NOT NULL, owner_kind TEXT NOT NULL CHECK(owner_kind IN ('tweet', 'youtube_video', 'instagram_reel', 'tiktok_video')), title TEXT, description TEXT, duration INTEGER, published_at INTEGER NOT NULL DEFAULT 0, downloaded_at INTEGER NOT NULL DEFAULT 0, is_temp INTEGER DEFAULT 0, is_pinned INTEGER DEFAULT 0, metadata_json TEXT, media_kind TEXT DEFAULT '', slide_count INTEGER DEFAULT 0, source_kind TEXT DEFAULT '', dearrow_title TEXT, dearrow_title_casual TEXT, dearrow_checked_at INTEGER );

-- table: watch_history on watch_history
CREATE TABLE watch_history ( video_id TEXT PRIMARY KEY, playback_position REAL NOT NULL DEFAULT 0, duration REAL, updated_at_ms INTEGER NOT NULL DEFAULT 0 );

-- trigger: android_sync_head_assets_delete on assets
CREATE TRIGGER android_sync_head_assets_delete AFTER DELETE ON assets WHEN TRIM(CAST(OLD.asset_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('asset', CAST(OLD.asset_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_assets_insert on assets
CREATE TRIGGER android_sync_head_assets_insert AFTER INSERT ON assets WHEN TRIM(CAST(NEW.asset_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('asset', CAST(NEW.asset_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_assets_update on assets
CREATE TRIGGER android_sync_head_assets_update AFTER UPDATE OF asset_id, asset_kind, owner_kind, owner_id, media_index, object_id, desired_object_id, lifecycle_state, revision, is_auto, audio_language, required_reason ON assets WHEN (OLD.asset_id IS NOT NEW.asset_id OR OLD.asset_kind IS NOT NEW.asset_kind OR OLD.owner_kind IS NOT NEW.owner_kind OR OLD.owner_id IS NOT NEW.owner_id OR OLD.media_index IS NOT NEW.media_index OR OLD.object_id IS NOT NEW.object_id OR OLD.desired_object_id IS NOT NEW.desired_object_id OR OLD.lifecycle_state IS NOT NEW.lifecycle_state OR OLD.revision IS NOT NEW.revision OR OLD.is_auto IS NOT NEW.is_auto OR OLD.audio_language IS NOT NEW.audio_language OR OLD.required_reason IS NOT NEW.required_reason) AND TRIM(CAST(NEW.asset_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.asset_id IS NOT NEW.asset_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'asset', CAST(OLD.asset_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.asset_id IS NOT NEW.asset_id AND TRIM(CAST(OLD.asset_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('asset', CAST(NEW.asset_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmark_categories_delete on bookmark_categories
CREATE TRIGGER android_sync_head_bookmark_categories_delete AFTER DELETE ON bookmark_categories WHEN TRIM(CAST(OLD.id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('bookmark_category', CAST(OLD.id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmark_categories_insert on bookmark_categories
CREATE TRIGGER android_sync_head_bookmark_categories_insert AFTER INSERT ON bookmark_categories WHEN TRIM(CAST(NEW.id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('bookmark_category', CAST(NEW.id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmark_categories_update on bookmark_categories
CREATE TRIGGER android_sync_head_bookmark_categories_update AFTER UPDATE OF id, name, archive_path, created_at ON bookmark_categories WHEN (OLD.id IS NOT NEW.id OR OLD.name IS NOT NEW.name OR OLD.archive_path IS NOT NEW.archive_path OR OLD.created_at IS NOT NEW.created_at) AND TRIM(CAST(NEW.id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.id IS NOT NEW.id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'bookmark_category', CAST(OLD.id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.id IS NOT NEW.id AND TRIM(CAST(OLD.id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('bookmark_category', CAST(NEW.id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmarks_delete on bookmarks
CREATE TRIGGER android_sync_head_bookmarks_delete AFTER DELETE ON bookmarks WHEN TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('bookmark', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmarks_hydrate_delete on bookmarks
CREATE TRIGGER android_sync_head_bookmarks_hydrate_delete AFTER DELETE ON bookmarks WHEN (1) AND TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmarks_hydrate_insert on bookmarks
CREATE TRIGGER android_sync_head_bookmarks_hydrate_insert AFTER INSERT ON bookmarks WHEN (1) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmarks_hydrate_update_new on bookmarks
CREATE TRIGGER android_sync_head_bookmarks_hydrate_update_new AFTER UPDATE OF video_id ON bookmarks WHEN (OLD.video_id IS NOT NEW.video_id) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmarks_hydrate_update_old on bookmarks
CREATE TRIGGER android_sync_head_bookmarks_hydrate_update_old AFTER UPDATE OF video_id ON bookmarks WHEN (OLD.video_id IS NOT NEW.video_id) AND TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmarks_insert on bookmarks
CREATE TRIGGER android_sync_head_bookmarks_insert AFTER INSERT ON bookmarks WHEN TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('bookmark', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_bookmarks_update on bookmarks
CREATE TRIGGER android_sync_head_bookmarks_update AFTER UPDATE OF video_id, category_id, custom_title, account_handles, media_indices, bookmarked_at ON bookmarks WHEN (OLD.video_id IS NOT NEW.video_id OR OLD.category_id IS NOT NEW.category_id OR OLD.custom_title IS NOT NEW.custom_title OR OLD.account_handles IS NOT NEW.account_handles OR OLD.media_indices IS NOT NEW.media_indices OR OLD.bookmarked_at IS NOT NEW.bookmarked_at) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'bookmark', CAST(OLD.video_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id AND TRIM(CAST(OLD.video_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('bookmark', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_follows_delete on channel_follows
CREATE TRIGGER android_sync_head_channel_follows_delete AFTER DELETE ON channel_follows WHEN TRIM(CAST(OLD.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_follow', CAST(OLD.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_follows_insert on channel_follows
CREATE TRIGGER android_sync_head_channel_follows_insert AFTER INSERT ON channel_follows WHEN TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_follow', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_follows_update on channel_follows
CREATE TRIGGER android_sync_head_channel_follows_update AFTER UPDATE OF channel_id, followed_at ON channel_follows WHEN (OLD.channel_id IS NOT NEW.channel_id OR OLD.followed_at IS NOT NEW.followed_at) AND TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'channel_follow', CAST(OLD.channel_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id AND TRIM(CAST(OLD.channel_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_follow', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_profiles_delete on channel_profiles
CREATE TRIGGER android_sync_head_channel_profiles_delete AFTER DELETE ON channel_profiles WHEN TRIM(CAST(OLD.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel', CAST(OLD.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_profiles_insert on channel_profiles
CREATE TRIGGER android_sync_head_channel_profiles_insert AFTER INSERT ON channel_profiles WHEN TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_profiles_update on channel_profiles
CREATE TRIGGER android_sync_head_channel_profiles_update AFTER UPDATE OF channel_id, platform, handle, display_name, bio, website, followers, following, verified, verified_type, protected, tombstone ON channel_profiles WHEN (OLD.channel_id IS NOT NEW.channel_id OR OLD.platform IS NOT NEW.platform OR OLD.handle IS NOT NEW.handle OR OLD.display_name IS NOT NEW.display_name OR OLD.bio IS NOT NEW.bio OR OLD.website IS NOT NEW.website OR OLD.followers IS NOT NEW.followers OR OLD.following IS NOT NEW.following OR OLD.verified IS NOT NEW.verified OR OLD.verified_type IS NOT NEW.verified_type OR OLD.protected IS NOT NEW.protected OR OLD.tombstone IS NOT NEW.tombstone) AND TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'channel', CAST(OLD.channel_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id AND TRIM(CAST(OLD.channel_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_settings_delete on channel_settings
CREATE TRIGGER android_sync_head_channel_settings_delete AFTER DELETE ON channel_settings WHEN TRIM(CAST(OLD.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_setting', CAST(OLD.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_settings_insert on channel_settings
CREATE TRIGGER android_sync_head_channel_settings_insert AFTER INSERT ON channel_settings WHEN TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_setting', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_settings_update on channel_settings
CREATE TRIGGER android_sync_head_channel_settings_update AFTER UPDATE OF channel_id, media_only, include_reposts, media_download_limit, max_videos, download_subtitles, updated_at ON channel_settings WHEN (OLD.channel_id IS NOT NEW.channel_id OR OLD.media_only IS NOT NEW.media_only OR OLD.include_reposts IS NOT NEW.include_reposts OR OLD.media_download_limit IS NOT NEW.media_download_limit OR OLD.max_videos IS NOT NEW.max_videos OR OLD.download_subtitles IS NOT NEW.download_subtitles OR OLD.updated_at IS NOT NEW.updated_at) AND TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'channel_setting', CAST(OLD.channel_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id AND TRIM(CAST(OLD.channel_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_setting', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_stars_delete on channel_stars
CREATE TRIGGER android_sync_head_channel_stars_delete AFTER DELETE ON channel_stars WHEN TRIM(CAST(OLD.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_star', CAST(OLD.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_stars_insert on channel_stars
CREATE TRIGGER android_sync_head_channel_stars_insert AFTER INSERT ON channel_stars WHEN TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_star', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channel_stars_update on channel_stars
CREATE TRIGGER android_sync_head_channel_stars_update AFTER UPDATE OF channel_id, starred_at ON channel_stars WHEN (OLD.channel_id IS NOT NEW.channel_id OR OLD.starred_at IS NOT NEW.starred_at) AND TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'channel_star', CAST(OLD.channel_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id AND TRIM(CAST(OLD.channel_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel_star', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channels_delete on channels
CREATE TRIGGER android_sync_head_channels_delete AFTER DELETE ON channels WHEN TRIM(CAST(OLD.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel', CAST(OLD.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channels_insert on channels
CREATE TRIGGER android_sync_head_channels_insert AFTER INSERT ON channels WHEN TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_channels_update on channels
CREATE TRIGGER android_sync_head_channels_update AFTER UPDATE OF channel_id, source_id, name, url, platform ON channels WHEN (OLD.channel_id IS NOT NEW.channel_id OR OLD.source_id IS NOT NEW.source_id OR OLD.name IS NOT NEW.name OR OLD.url IS NOT NEW.url OR OLD.platform IS NOT NEW.platform) AND TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'channel', CAST(OLD.channel_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id AND TRIM(CAST(OLD.channel_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('channel', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_items_delete on feed_items
CREATE TRIGGER android_sync_head_feed_items_delete AFTER DELETE ON feed_items WHEN TRIM(CAST(OLD.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_items_insert on feed_items
CREATE TRIGGER android_sync_head_feed_items_insert AFTER INSERT ON feed_items WHEN TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_items_update on feed_items
CREATE TRIGGER android_sync_head_feed_items_update AFTER UPDATE OF tweet_id, body_text, lang, is_retweet, quote_tweet_id, quote_body_text, quote_lang, quote_media_json, media_json, canonical_url, reply_to_status, is_reply, is_ghost, quote_published_at, views, likes, retweets, published_at, content_hash, canonical_tweet_id, source_channel_id, channel_id, reposter_channel_id, quote_channel_id, reply_channel_id ON feed_items WHEN (OLD.tweet_id IS NOT NEW.tweet_id OR OLD.body_text IS NOT NEW.body_text OR OLD.lang IS NOT NEW.lang OR OLD.is_retweet IS NOT NEW.is_retweet OR OLD.quote_tweet_id IS NOT NEW.quote_tweet_id OR OLD.quote_body_text IS NOT NEW.quote_body_text OR OLD.quote_lang IS NOT NEW.quote_lang OR OLD.quote_media_json IS NOT NEW.quote_media_json OR OLD.media_json IS NOT NEW.media_json OR OLD.canonical_url IS NOT NEW.canonical_url OR OLD.reply_to_status IS NOT NEW.reply_to_status OR OLD.is_reply IS NOT NEW.is_reply OR OLD.is_ghost IS NOT NEW.is_ghost OR OLD.quote_published_at IS NOT NEW.quote_published_at OR OLD.views IS NOT NEW.views OR OLD.likes IS NOT NEW.likes OR OLD.retweets IS NOT NEW.retweets OR OLD.published_at IS NOT NEW.published_at OR OLD.content_hash IS NOT NEW.content_hash OR OLD.canonical_tweet_id IS NOT NEW.canonical_tweet_id OR OLD.source_channel_id IS NOT NEW.source_channel_id OR OLD.channel_id IS NOT NEW.channel_id OR OLD.reposter_channel_id IS NOT NEW.reposter_channel_id OR OLD.quote_channel_id IS NOT NEW.quote_channel_id OR OLD.reply_channel_id IS NOT NEW.reply_channel_id) AND TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed', CAST(OLD.tweet_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id AND TRIM(CAST(OLD.tweet_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_likes_delete on feed_likes
CREATE TRIGGER android_sync_head_feed_likes_delete AFTER DELETE ON feed_likes WHEN TRIM(CAST(OLD.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_like', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_likes_hydrate_delete on feed_likes
CREATE TRIGGER android_sync_head_feed_likes_hydrate_delete AFTER DELETE ON feed_likes WHEN (1) AND TRIM(CAST(OLD.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_likes_hydrate_insert on feed_likes
CREATE TRIGGER android_sync_head_feed_likes_hydrate_insert AFTER INSERT ON feed_likes WHEN (1) AND TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_likes_hydrate_update_new on feed_likes
CREATE TRIGGER android_sync_head_feed_likes_hydrate_update_new AFTER UPDATE OF tweet_id ON feed_likes WHEN (OLD.tweet_id IS NOT NEW.tweet_id) AND TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_likes_hydrate_update_old on feed_likes
CREATE TRIGGER android_sync_head_feed_likes_hydrate_update_old AFTER UPDATE OF tweet_id ON feed_likes WHEN (OLD.tweet_id IS NOT NEW.tweet_id) AND TRIM(CAST(OLD.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_likes_insert on feed_likes
CREATE TRIGGER android_sync_head_feed_likes_insert AFTER INSERT ON feed_likes WHEN TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_like', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_likes_update on feed_likes
CREATE TRIGGER android_sync_head_feed_likes_update AFTER UPDATE OF tweet_id, liked_at ON feed_likes WHEN (OLD.tweet_id IS NOT NEW.tweet_id OR OLD.liked_at IS NOT NEW.liked_at) AND TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed_like', CAST(OLD.tweet_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id AND TRIM(CAST(OLD.tweet_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_like', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_old_hash_delete on feed_items
CREATE TRIGGER android_sync_head_feed_old_hash_delete AFTER DELETE ON feed_items WHEN COALESCE(OLD.content_hash, '') != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items peer WHERE peer.content_hash = OLD.content_hash); INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed', (SELECT MIN(peer.tweet_id) FROM feed_items peer WHERE peer.content_hash = OLD.content_hash), revision FROM android_sync_clock WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items peer WHERE peer.content_hash = OLD.content_hash) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_old_hash_update on feed_items
CREATE TRIGGER android_sync_head_feed_old_hash_update AFTER UPDATE OF content_hash ON feed_items WHEN OLD.content_hash IS NOT NEW.content_hash AND COALESCE(OLD.content_hash, '') != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items peer WHERE peer.content_hash = OLD.content_hash AND peer.tweet_id != OLD.tweet_id); INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed', (SELECT MIN(peer.tweet_id) FROM feed_items peer WHERE peer.content_hash = OLD.content_hash AND peer.tweet_id != OLD.tweet_id), revision FROM android_sync_clock WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items peer WHERE peer.content_hash = OLD.content_hash AND peer.tweet_id != OLD.tweet_id) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_old_quote_delete on feed_items
CREATE TRIGGER android_sync_head_feed_old_quote_delete AFTER DELETE ON feed_items WHEN COALESCE(OLD.quote_tweet_id, '') != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items WHERE tweet_id = OLD.quote_tweet_id); INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed', OLD.quote_tweet_id, revision FROM android_sync_clock WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items WHERE tweet_id = OLD.quote_tweet_id) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_old_quote_update on feed_items
CREATE TRIGGER android_sync_head_feed_old_quote_update AFTER UPDATE OF quote_tweet_id ON feed_items WHEN OLD.quote_tweet_id IS NOT NEW.quote_tweet_id AND COALESCE(OLD.quote_tweet_id, '') != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items WHERE tweet_id = OLD.quote_tweet_id); INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed', OLD.quote_tweet_id, revision FROM android_sync_clock WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items WHERE tweet_id = OLD.quote_tweet_id) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_old_reply_delete on feed_items
CREATE TRIGGER android_sync_head_feed_old_reply_delete AFTER DELETE ON feed_items WHEN COALESCE(OLD.reply_to_status, '') != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items WHERE tweet_id = OLD.reply_to_status); INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed', OLD.reply_to_status, revision FROM android_sync_clock WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items WHERE tweet_id = OLD.reply_to_status) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_old_reply_update on feed_items
CREATE TRIGGER android_sync_head_feed_old_reply_update AFTER UPDATE OF reply_to_status ON feed_items WHEN OLD.reply_to_status IS NOT NEW.reply_to_status AND COALESCE(OLD.reply_to_status, '') != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items WHERE tweet_id = OLD.reply_to_status); INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed', OLD.reply_to_status, revision FROM android_sync_clock WHERE id = 1 AND EXISTS (SELECT 1 FROM feed_items WHERE tweet_id = OLD.reply_to_status) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_rank_snapshot_delete on feed_rank_snapshot
CREATE TRIGGER android_sync_head_feed_rank_snapshot_delete AFTER DELETE ON feed_rank_snapshot WHEN TRIM(CAST(OLD.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_rank', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_rank_snapshot_insert on feed_rank_snapshot
CREATE TRIGGER android_sync_head_feed_rank_snapshot_insert AFTER INSERT ON feed_rank_snapshot WHEN TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_rank', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_rank_snapshot_update on feed_rank_snapshot
CREATE TRIGGER android_sync_head_feed_rank_snapshot_update AFTER UPDATE OF tweet_id, rank_position, base_score, decay_factor, freshness_bonus, jitter, diversity_demoted_by, final_score, computed_at ON feed_rank_snapshot WHEN (OLD.tweet_id IS NOT NEW.tweet_id OR OLD.rank_position IS NOT NEW.rank_position OR OLD.base_score IS NOT NEW.base_score OR OLD.decay_factor IS NOT NEW.decay_factor OR OLD.freshness_bonus IS NOT NEW.freshness_bonus OR OLD.jitter IS NOT NEW.jitter OR OLD.diversity_demoted_by IS NOT NEW.diversity_demoted_by OR OLD.final_score IS NOT NEW.final_score OR OLD.computed_at IS NOT NEW.computed_at) AND TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed_rank', CAST(OLD.tweet_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id AND TRIM(CAST(OLD.tweet_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_rank', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_seen_delete on feed_seen
CREATE TRIGGER android_sync_head_feed_seen_delete AFTER DELETE ON feed_seen WHEN TRIM(CAST(OLD.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_seen', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_seen_insert on feed_seen
CREATE TRIGGER android_sync_head_feed_seen_insert AFTER INSERT ON feed_seen WHEN TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_seen', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_feed_seen_update on feed_seen
CREATE TRIGGER android_sync_head_feed_seen_update AFTER UPDATE OF tweet_id, seen_at ON feed_seen WHEN (OLD.tweet_id IS NOT NEW.tweet_id OR OLD.seen_at IS NOT NEW.seen_at) AND TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed_seen', CAST(OLD.tweet_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id AND TRIM(CAST(OLD.tweet_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed_seen', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_moment_views_delete on moment_views
CREATE TRIGGER android_sync_head_moment_views_delete AFTER DELETE ON moment_views WHEN TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('moment_view', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_moment_views_insert on moment_views
CREATE TRIGGER android_sync_head_moment_views_insert AFTER INSERT ON moment_views WHEN TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('moment_view', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_moment_views_update on moment_views
CREATE TRIGGER android_sync_head_moment_views_update AFTER UPDATE OF video_id, viewed_at ON moment_views WHEN (OLD.video_id IS NOT NEW.video_id OR OLD.viewed_at IS NOT NEW.viewed_at) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'moment_view', CAST(OLD.video_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id AND TRIM(CAST(OLD.video_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('moment_view', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_moments_cursors_delete on moments_cursors
CREATE TRIGGER android_sync_head_moments_cursors_delete AFTER DELETE ON moments_cursors WHEN TRIM(CAST(OLD.scope AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('moments_cursor', CAST(OLD.scope AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_moments_cursors_insert on moments_cursors
CREATE TRIGGER android_sync_head_moments_cursors_insert AFTER INSERT ON moments_cursors WHEN TRIM(CAST(NEW.scope AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('moments_cursor', CAST(NEW.scope AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_moments_cursors_update on moments_cursors
CREATE TRIGGER android_sync_head_moments_cursors_update AFTER UPDATE OF scope, video_id, position_ms, sort_at_ms, updated_at_ms ON moments_cursors WHEN (OLD.scope IS NOT NEW.scope OR OLD.video_id IS NOT NEW.video_id OR OLD.position_ms IS NOT NEW.position_ms OR OLD.sort_at_ms IS NOT NEW.sort_at_ms OR OLD.updated_at_ms IS NOT NEW.updated_at_ms) AND TRIM(CAST(NEW.scope AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.scope IS NOT NEW.scope; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'moments_cursor', CAST(OLD.scope AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.scope IS NOT NEW.scope AND TRIM(CAST(OLD.scope AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('moments_cursor', CAST(NEW.scope AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_muted_channels_delete on muted_channels
CREATE TRIGGER android_sync_head_muted_channels_delete AFTER DELETE ON muted_channels WHEN TRIM(CAST(OLD.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('muted_channel', CAST(OLD.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_muted_channels_insert on muted_channels
CREATE TRIGGER android_sync_head_muted_channels_insert AFTER INSERT ON muted_channels WHEN TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('muted_channel', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_muted_channels_update on muted_channels
CREATE TRIGGER android_sync_head_muted_channels_update AFTER UPDATE OF channel_id, muted_at ON muted_channels WHEN (OLD.channel_id IS NOT NEW.channel_id OR OLD.muted_at IS NOT NEW.muted_at) AND TRIM(CAST(NEW.channel_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'muted_channel', CAST(OLD.channel_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.channel_id IS NOT NEW.channel_id AND TRIM(CAST(OLD.channel_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('muted_channel', CAST(NEW.channel_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_retweet_sources_delete on retweet_sources
CREATE TRIGGER android_sync_head_retweet_sources_delete AFTER DELETE ON retweet_sources WHEN TRIM(CAST(OLD.content_hash AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('retweet_sources', CAST(OLD.content_hash AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_retweet_sources_insert on retweet_sources
CREATE TRIGGER android_sync_head_retweet_sources_insert AFTER INSERT ON retweet_sources WHEN TRIM(CAST(NEW.content_hash AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('retweet_sources', CAST(NEW.content_hash AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_retweet_sources_update on retweet_sources
CREATE TRIGGER android_sync_head_retweet_sources_update AFTER UPDATE OF content_hash, retweeter_channel_id, tweet_id, published_at ON retweet_sources WHEN (OLD.content_hash IS NOT NEW.content_hash OR OLD.retweeter_channel_id IS NOT NEW.retweeter_channel_id OR OLD.tweet_id IS NOT NEW.tweet_id OR OLD.published_at IS NOT NEW.published_at) AND TRIM(CAST(NEW.content_hash AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.content_hash IS NOT NEW.content_hash; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'retweet_sources', CAST(OLD.content_hash AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.content_hash IS NOT NEW.content_hash AND TRIM(CAST(OLD.content_hash AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('retweet_sources', CAST(NEW.content_hash AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_settings_delete on settings
CREATE TRIGGER android_sync_head_settings_delete AFTER DELETE ON settings WHEN OLD.key IN ('moments_include_reposts_default', 'instagram_include_tagged_default', 'include_reposts_default', 'translate_target_lang', 'translate_skip_langs') BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('setting', OLD.key, (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_settings_insert on settings
CREATE TRIGGER android_sync_head_settings_insert AFTER INSERT ON settings WHEN NEW.key IN ('moments_include_reposts_default', 'instagram_include_tagged_default', 'include_reposts_default', 'translate_target_lang', 'translate_skip_langs') BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('setting', NEW.key, (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_settings_update on settings
CREATE TRIGGER android_sync_head_settings_update AFTER UPDATE OF key, value ON settings WHEN (OLD.key IS NOT NEW.key OR OLD.value IS NOT NEW.value) AND (OLD.key IN ('moments_include_reposts_default', 'instagram_include_tagged_default', 'include_reposts_default', 'translate_target_lang', 'translate_skip_langs') OR NEW.key IN ('moments_include_reposts_default', 'instagram_include_tagged_default', 'include_reposts_default', 'translate_target_lang', 'translate_skip_langs')) BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.key IS NOT NEW.key AND OLD.key IN ('moments_include_reposts_default', 'instagram_include_tagged_default', 'include_reposts_default', 'translate_target_lang', 'translate_skip_langs'); INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'setting', OLD.key, revision FROM android_sync_clock WHERE id = 1 AND OLD.key IS NOT NEW.key AND OLD.key IN ('moments_include_reposts_default', 'instagram_include_tagged_default', 'include_reposts_default', 'translate_target_lang', 'translate_skip_langs') ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND NEW.key IN ('moments_include_reposts_default', 'instagram_include_tagged_default', 'include_reposts_default', 'translate_target_lang', 'translate_skip_langs'); INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'setting', NEW.key, revision FROM android_sync_clock WHERE id = 1 AND NEW.key IN ('moments_include_reposts_default', 'instagram_include_tagged_default', 'include_reposts_default', 'translate_target_lang', 'translate_skip_langs') ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_sponsorblock_checked_delete on sponsorblock_checked
CREATE TRIGGER android_sync_head_sponsorblock_checked_delete AFTER DELETE ON sponsorblock_checked WHEN TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_sponsorblock_checked_insert on sponsorblock_checked
CREATE TRIGGER android_sync_head_sponsorblock_checked_insert AFTER INSERT ON sponsorblock_checked WHEN TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_sponsorblock_checked_update on sponsorblock_checked
CREATE TRIGGER android_sync_head_sponsorblock_checked_update AFTER UPDATE OF video_id, checked_at, video_age_at_check ON sponsorblock_checked WHEN (OLD.video_id IS NOT NEW.video_id OR OLD.checked_at IS NOT NEW.checked_at OR OLD.video_age_at_check IS NOT NEW.video_age_at_check) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'video', CAST(OLD.video_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id AND TRIM(CAST(OLD.video_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_sponsorblock_segments_delete on sponsorblock_segments
CREATE TRIGGER android_sync_head_sponsorblock_segments_delete AFTER DELETE ON sponsorblock_segments WHEN TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_sponsorblock_segments_insert on sponsorblock_segments
CREATE TRIGGER android_sync_head_sponsorblock_segments_insert AFTER INSERT ON sponsorblock_segments WHEN TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_sponsorblock_segments_update on sponsorblock_segments
CREATE TRIGGER android_sync_head_sponsorblock_segments_update AFTER UPDATE OF video_id, start_time, end_time, category ON sponsorblock_segments WHEN (OLD.video_id IS NOT NEW.video_id OR OLD.start_time IS NOT NEW.start_time OR OLD.end_time IS NOT NEW.end_time OR OLD.category IS NOT NEW.category) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'video', CAST(OLD.video_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id AND TRIM(CAST(OLD.video_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_translations_delete on translations
CREATE TRIGGER android_sync_head_translations_delete AFTER DELETE ON translations WHEN TRIM(CAST(OLD.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(OLD.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_translations_insert on translations
CREATE TRIGGER android_sync_head_translations_insert AFTER INSERT ON translations WHEN TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_translations_update on translations
CREATE TRIGGER android_sync_head_translations_update AFTER UPDATE OF tweet_id, field, source_lang, target_lang, translated_text, translated_at ON translations WHEN (OLD.tweet_id IS NOT NEW.tweet_id OR OLD.field IS NOT NEW.field OR OLD.source_lang IS NOT NEW.source_lang OR OLD.target_lang IS NOT NEW.target_lang OR OLD.translated_text IS NOT NEW.translated_text OR OLD.translated_at IS NOT NEW.translated_at) AND TRIM(CAST(NEW.tweet_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'feed', CAST(OLD.tweet_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.tweet_id IS NOT NEW.tweet_id AND TRIM(CAST(OLD.tweet_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('feed', CAST(NEW.tweet_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_video_comments_delete on video_comments
CREATE TRIGGER android_sync_head_video_comments_delete AFTER DELETE ON video_comments WHEN TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_video_comments_insert on video_comments
CREATE TRIGGER android_sync_head_video_comments_insert AFTER INSERT ON video_comments WHEN TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_video_comments_update on video_comments
CREATE TRIGGER android_sync_head_video_comments_update AFTER UPDATE OF video_id, comment_id, parent_id, author_name, author_id, text, like_count, published_at ON video_comments WHEN (OLD.video_id IS NOT NEW.video_id OR OLD.comment_id IS NOT NEW.comment_id OR OLD.parent_id IS NOT NEW.parent_id OR OLD.author_name IS NOT NEW.author_name OR OLD.author_id IS NOT NEW.author_id OR OLD.text IS NOT NEW.text OR OLD.like_count IS NOT NEW.like_count OR OLD.published_at IS NOT NEW.published_at) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'video', CAST(OLD.video_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id AND TRIM(CAST(OLD.video_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_video_repost_sources_delete on video_repost_sources
CREATE TRIGGER android_sync_head_video_repost_sources_delete AFTER DELETE ON video_repost_sources WHEN TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_video_repost_sources_insert on video_repost_sources
CREATE TRIGGER android_sync_head_video_repost_sources_insert AFTER INSERT ON video_repost_sources WHEN TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_video_repost_sources_update on video_repost_sources
CREATE TRIGGER android_sync_head_video_repost_sources_update AFTER UPDATE OF video_id, reposter_channel_id, reposted_at_ms, first_seen_at_ms, updated_at_ms ON video_repost_sources WHEN (OLD.video_id IS NOT NEW.video_id OR OLD.reposter_channel_id IS NOT NEW.reposter_channel_id OR OLD.reposted_at_ms IS NOT NEW.reposted_at_ms OR OLD.first_seen_at_ms IS NOT NEW.first_seen_at_ms OR OLD.updated_at_ms IS NOT NEW.updated_at_ms) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'video', CAST(OLD.video_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id AND TRIM(CAST(OLD.video_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_videos_delete on videos
CREATE TRIGGER android_sync_head_videos_delete AFTER DELETE ON videos WHEN TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_videos_insert on videos
CREATE TRIGGER android_sync_head_videos_insert AFTER INSERT ON videos WHEN TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_videos_update on videos
CREATE TRIGGER android_sync_head_videos_update AFTER UPDATE OF video_id, channel_id, owner_kind, title, description, duration, published_at, metadata_json, media_kind, slide_count, source_kind, dearrow_title, dearrow_title_casual ON videos WHEN (OLD.video_id IS NOT NEW.video_id OR OLD.channel_id IS NOT NEW.channel_id OR OLD.owner_kind IS NOT NEW.owner_kind OR OLD.title IS NOT NEW.title OR OLD.description IS NOT NEW.description OR OLD.duration IS NOT NEW.duration OR OLD.published_at IS NOT NEW.published_at OR OLD.metadata_json IS NOT NEW.metadata_json OR OLD.media_kind IS NOT NEW.media_kind OR OLD.slide_count IS NOT NEW.slide_count OR OLD.source_kind IS NOT NEW.source_kind OR OLD.dearrow_title IS NOT NEW.dearrow_title OR OLD.dearrow_title_casual IS NOT NEW.dearrow_title_casual) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'video', CAST(OLD.video_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id AND TRIM(CAST(OLD.video_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('video', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_watch_history_delete on watch_history
CREATE TRIGGER android_sync_head_watch_history_delete AFTER DELETE ON watch_history WHEN TRIM(CAST(OLD.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('watch_history', CAST(OLD.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_watch_history_insert on watch_history
CREATE TRIGGER android_sync_head_watch_history_insert AFTER INSERT ON watch_history WHEN TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('watch_history', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: android_sync_head_watch_history_update on watch_history
CREATE TRIGGER android_sync_head_watch_history_update AFTER UPDATE OF video_id, playback_position, duration, updated_at_ms ON watch_history WHEN (OLD.video_id IS NOT NEW.video_id OR OLD.playback_position IS NOT NEW.playback_position OR OLD.duration IS NOT NEW.duration OR OLD.updated_at_ms IS NOT NEW.updated_at_ms) AND TRIM(CAST(NEW.video_id AS TEXT)) != '' BEGIN UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) SELECT 'watch_history', CAST(OLD.video_id AS TEXT), revision FROM android_sync_clock WHERE id = 1 AND OLD.video_id IS NOT NEW.video_id AND TRIM(CAST(OLD.video_id AS TEXT)) != '' ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; UPDATE android_sync_clock SET revision = revision + 1 WHERE id = 1; INSERT INTO android_sync_heads (owner_kind, owner_id, revision) VALUES ('watch_history', CAST(NEW.video_id AS TEXT), (SELECT revision FROM android_sync_clock WHERE id = 1)) ON CONFLICT(owner_kind, owner_id) DO UPDATE SET revision = excluded.revision; END;

-- trigger: trg_search_channel_profiles_ad on channel_profiles
CREATE TRIGGER trg_search_channel_profiles_ad AFTER DELETE ON channel_profiles BEGIN DELETE FROM search_channels_fts WHERE rowid = (SELECT id FROM channels WHERE channel_id = old.channel_id); INSERT INTO search_channels_fts(rowid, channel_id_pk, name, source_id, display_name, handle) SELECT c.id, c.channel_id, COALESCE(c.name, ''), COALESCE(c.source_id, ''), '', '' FROM channels c WHERE c.channel_id = old.channel_id; DELETE FROM search_videos_fts WHERE rowid IN (SELECT id FROM videos WHERE channel_id = old.channel_id); INSERT INTO search_videos_fts(rowid, video_id_pk, title, dearrow_title, dearrow_title_casual, channel_name) SELECT v.id, v.video_id, COALESCE(v.title, ''), COALESCE(v.dearrow_title, ''), COALESCE(v.dearrow_title_casual, ''), '' FROM videos v WHERE v.channel_id = old.channel_id; END;

-- trigger: trg_search_channel_profiles_ai on channel_profiles
CREATE TRIGGER trg_search_channel_profiles_ai AFTER INSERT ON channel_profiles BEGIN DELETE FROM search_channels_fts WHERE rowid = (SELECT id FROM channels WHERE channel_id = new.channel_id); INSERT INTO search_channels_fts(rowid, channel_id_pk, name, source_id, display_name, handle) SELECT c.id, c.channel_id, COALESCE(c.name, ''), COALESCE(c.source_id, ''), COALESCE(cp.display_name, ''), COALESCE(cp.handle, '') FROM channels c LEFT JOIN channel_profiles cp ON cp.channel_id = c.channel_id AND COALESCE(cp.tombstone, 0) = 0 WHERE c.channel_id = new.channel_id; DELETE FROM search_videos_fts WHERE rowid IN (SELECT id FROM videos WHERE channel_id = new.channel_id); INSERT INTO search_videos_fts(rowid, video_id_pk, title, dearrow_title, dearrow_title_casual, channel_name) SELECT v.id, v.video_id, COALESCE(v.title, ''), COALESCE(v.dearrow_title, ''), COALESCE(v.dearrow_title_casual, ''), COALESCE(new.display_name, '') FROM videos v WHERE v.channel_id = new.channel_id; END;

-- trigger: trg_search_channel_profiles_au on channel_profiles
CREATE TRIGGER trg_search_channel_profiles_au AFTER UPDATE ON channel_profiles BEGIN DELETE FROM search_channels_fts WHERE rowid = (SELECT id FROM channels WHERE channel_id = new.channel_id); INSERT INTO search_channels_fts(rowid, channel_id_pk, name, source_id, display_name, handle) SELECT c.id, c.channel_id, COALESCE(c.name, ''), COALESCE(c.source_id, ''), COALESCE(cp.display_name, ''), COALESCE(cp.handle, '') FROM channels c LEFT JOIN channel_profiles cp ON cp.channel_id = c.channel_id AND COALESCE(cp.tombstone, 0) = 0 WHERE c.channel_id = new.channel_id; DELETE FROM search_videos_fts WHERE rowid IN (SELECT id FROM videos WHERE channel_id = new.channel_id); INSERT INTO search_videos_fts(rowid, video_id_pk, title, dearrow_title, dearrow_title_casual, channel_name) SELECT v.id, v.video_id, COALESCE(v.title, ''), COALESCE(v.dearrow_title, ''), COALESCE(v.dearrow_title_casual, ''), COALESCE(new.display_name, '') FROM videos v WHERE v.channel_id = new.channel_id; END;

-- trigger: trg_search_channels_ad on channels
CREATE TRIGGER trg_search_channels_ad AFTER DELETE ON channels BEGIN DELETE FROM search_channels_fts WHERE rowid = old.id; END;

-- trigger: trg_search_channels_ai on channels
CREATE TRIGGER trg_search_channels_ai AFTER INSERT ON channels BEGIN INSERT INTO search_channels_fts(rowid, channel_id_pk, name, source_id, display_name, handle) VALUES ( new.id, new.channel_id, COALESCE(new.name, ''), COALESCE(new.source_id, ''), COALESCE((SELECT display_name FROM channel_profiles WHERE channel_id = new.channel_id AND COALESCE(tombstone, 0) = 0), ''), COALESCE((SELECT handle FROM channel_profiles WHERE channel_id = new.channel_id AND COALESCE(tombstone, 0) = 0), '') ); END;

-- trigger: trg_search_channels_au on channels
CREATE TRIGGER trg_search_channels_au AFTER UPDATE ON channels BEGIN DELETE FROM search_channels_fts WHERE rowid = old.id; INSERT INTO search_channels_fts(rowid, channel_id_pk, name, source_id, display_name, handle) VALUES ( new.id, new.channel_id, COALESCE(new.name, ''), COALESCE(new.source_id, ''), COALESCE((SELECT display_name FROM channel_profiles WHERE channel_id = new.channel_id AND COALESCE(tombstone, 0) = 0), ''), COALESCE((SELECT handle FROM channel_profiles WHERE channel_id = new.channel_id AND COALESCE(tombstone, 0) = 0), '') ); END;

-- trigger: trg_search_videos_ad on videos
CREATE TRIGGER trg_search_videos_ad AFTER DELETE ON videos BEGIN DELETE FROM search_videos_fts WHERE rowid = old.id; END;

-- trigger: trg_search_videos_ai on videos
CREATE TRIGGER trg_search_videos_ai AFTER INSERT ON videos BEGIN INSERT INTO search_videos_fts(rowid, video_id_pk, title, dearrow_title, dearrow_title_casual, channel_name) VALUES ( new.id, new.video_id, COALESCE(new.title, ''), COALESCE(new.dearrow_title, ''), COALESCE(new.dearrow_title_casual, ''), COALESCE((SELECT display_name FROM channel_profiles WHERE channel_id = new.channel_id), '') ); END;

-- trigger: trg_search_videos_au on videos
CREATE TRIGGER trg_search_videos_au AFTER UPDATE ON videos BEGIN DELETE FROM search_videos_fts WHERE rowid = old.id; INSERT INTO search_videos_fts(rowid, video_id_pk, title, dearrow_title, dearrow_title_casual, channel_name) VALUES ( new.id, new.video_id, COALESCE(new.title, ''), COALESCE(new.dearrow_title, ''), COALESCE(new.dearrow_title_casual, ''), COALESCE((SELECT display_name FROM channel_profiles WHERE channel_id = new.channel_id), '') ); END;

-- view: feed_items_resolved on feed_items_resolved
CREATE VIEW feed_items_resolved AS SELECT fi.*, COALESCE(source_profile.handle, '') AS source_handle, COALESCE(author_profile.handle, '') AS author_handle, COALESCE(author_profile.display_name, '') AS author_display_name, CASE WHEN COALESCE(fi.channel_id, '') = '' THEN '' ELSE '/api/media/avatar/' || fi.channel_id END AS author_avatar_url, COALESCE(reposter_profile.handle, '') AS retweeted_by_handle, COALESCE(reposter_profile.display_name, '') AS retweeted_by_display_name, COALESCE(quote_profile.display_name, '') AS quote_author_display_name, COALESCE(quote_profile.handle, '') AS quote_author_handle, CASE WHEN COALESCE(fi.quote_channel_id, '') = '' THEN '' ELSE '/api/media/avatar/' || fi.quote_channel_id END AS quote_author_avatar_url, COALESCE(reply_profile.handle, '') AS reply_to_handle FROM feed_items fi LEFT JOIN channel_profiles source_profile ON source_profile.channel_id = fi.source_channel_id AND source_profile.tombstone = 0 LEFT JOIN channel_profiles author_profile ON author_profile.channel_id = fi.channel_id AND author_profile.tombstone = 0 LEFT JOIN channel_profiles reposter_profile ON reposter_profile.channel_id = fi.reposter_channel_id AND reposter_profile.tombstone = 0 LEFT JOIN channel_profiles quote_profile ON quote_profile.channel_id = fi.quote_channel_id AND quote_profile.tombstone = 0 LEFT JOIN channel_profiles reply_profile ON reply_profile.channel_id = fi.reply_channel_id AND reply_profile.tombstone = 0;

-- view: retweet_sources_resolved on retweet_sources_resolved
CREATE VIEW retweet_sources_resolved AS SELECT rs.content_hash, rs.retweeter_channel_id, COALESCE(cp.handle, '') AS retweeter_handle, cp.display_name AS retweeter_display_name, rs.tweet_id, rs.published_at FROM retweet_sources rs LEFT JOIN channel_profiles cp ON cp.channel_id = rs.retweeter_channel_id AND cp.tombstone = 0;

-- view: video_repost_sources_resolved on video_repost_sources_resolved
CREATE VIEW video_repost_sources_resolved AS SELECT vrs.video_id, vrs.reposter_channel_id, COALESCE(cp.handle, '') AS reposter_handle, cp.display_name AS reposter_display_name, vrs.reposted_at_ms, vrs.first_seen_at_ms, vrs.updated_at_ms FROM video_repost_sources vrs LEFT JOIN channel_profiles cp ON cp.channel_id = vrs.reposter_channel_id AND cp.tombstone = 0;
